# CI integration

This repository does not define a full CI pipeline.

CI logic, runners, signing, scanning and provenance
are implemented in `ai-secure-ci-cd-platform`.

This directory provides only:
- Entry points consumed by the central CI platform
- LLM-specific security and integrity policies
