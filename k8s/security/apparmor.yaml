apiVersion: v1
kind: Pod
metadata:
  name: apparmor-placeholder
  namespace: ai-llm
  annotations:
    container.apparmor.security.beta.kubernetes.io/placeholder: runtime/default
spec:
  containers:
    - name: placeholder
      image: gcr.io/distroless/base
      command: ["true"]
