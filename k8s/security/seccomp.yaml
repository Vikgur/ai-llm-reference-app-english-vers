apiVersion: v1
kind: Pod
metadata:
  name: seccomp-placeholder
  namespace: ai-llm
spec:
  securityContext:
    seccompProfile:
      type: RuntimeDefault
  containers:
    - name: placeholder
      image: gcr.io/distroless/base
      command: ["true"]
