apiVersion: v1
kind: LimitRange
metadata:
  name: ai-llm-limits
  namespace: ai-llm
spec:
  limits:
    - type: Container
      default:
        cpu: 500m
        memory: 512Mi
      defaultRequest:
        cpu: 100m
        memory: 256Mi
